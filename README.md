# m
m is a menu for the terminal that collects all applications on the system. Useful for i3. Whether from the repo or from flatpaks. Simply copy it to /usr/bin and call it up in the terminal with m. 

<b>Build</b>

gcc main.c -o m -lncurses
cp ./m /usr/bin
